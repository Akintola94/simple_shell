#include "main.h"

/**
 * add - adds two variables
 * @no1: the first number to be added
 * @no2: the second number to be added
 * Return: The added values
 */

int add(int no1, int no2)
{
	return (no1 + no2);
}
